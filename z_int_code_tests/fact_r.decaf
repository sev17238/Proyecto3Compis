class Program {

    int factorial(int n) {
        if (n == 1) {
            return n;
        } else {
            int fact;
            fact = n * factorial(n - 1);
            return fact
        }
    }

    void main() {
        int n;
        n = 5;
        factorial(n);
    }
}